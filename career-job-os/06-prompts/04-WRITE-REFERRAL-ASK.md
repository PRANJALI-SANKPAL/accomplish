# Prompt 04 — WRITE REFERRAL ASK

```text
Read AGENT-CONTRACT + MASTER-TRUTH.

TASK: Write a short LinkedIn referral request.

INPUTS:
- Employee first name:
- Company:
- Their role (if known):
- Job title + Job ID + link:
- Why fit in 2 true sentences (from Master Truth):

OUTPUT:
1. Connection note (≤300 chars) OR message if already connected
2. Full referral message (≤120 words)
3. 2-sentence blurb they can paste into referral form
4. Follow-up message if no reply in 5 days (polite, one try)

Rules: no flattery essay, no fake mutual history, include Job ID, give them an easy out.
```
